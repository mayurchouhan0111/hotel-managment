rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Hotel staff management rules
    // Allow authenticated staff users to read and write records
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
